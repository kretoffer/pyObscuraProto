name: Auto Tests

on:
  push:
    branches:
      - main
      - develop
      - 'hotfix/*'
      - 'release/*'
  pull_request:
    branches:
      - main
      - develop
      - 'release/*'

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.14'

    - name: Install build dependencies (CMake, etc.)
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r dev-requirements.txt
        pip install pybind11 wheel setuptools cmake

    - name: Build and install project
      run: |
        pip install -e .

    - name: Run tests
      run: |
        pytest